
// Define a variable to keep track of the current value
let value = 2;

// Set the loop condition to keep going while the value is less than or equal to 65536
while (value <= 65536) {
    // Output the current value
    console.log(value);

    value *= 2;
}